```json
{
  "status": "chart",
  "name": "",
  "sql": "SELECT c.fullname AS coursename, COUNT(ue.id) AS total FROM {course} c JOIN {enrol} e ON e.courseid = c.id JOIN {user_enrolments} ue ON ue.enrolid = e.id WHERE c.id <> :siteid GROUP BY c.id, c.fullname ORDER BY total DESC",
  "params": {"siteid": 1},
  "chart": {
    "type": "bar",
    "title": "Users per course",
    "labelcolumn": "coursename",
    "labelformat": "text",
    "series": [{"column": "total", "label": "Users"}]
  },
  "notes": "The site course is excluded."
}
```
